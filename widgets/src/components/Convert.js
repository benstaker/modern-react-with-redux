import React from 'react';

const Convert = ({ language, text }) => {
    return (
        <div>
            language: {language}
            text: {text}
        </div>
    );
};

export default Convert;
